# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates
#
# Dependabot is intentionally disabled on this fork: dependency updates are
# handled by Renovate (see renovate.json + .github/workflows/renovate.yaml).
# Keeping `updates: []` avoids duplicate dependency PRs from two bots.

version: 2
updates: []
